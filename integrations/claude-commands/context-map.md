# /context-map

Map this repository for efficient AI context use.

Run:

```bash
contextopt init
contextopt map .
contextopt export --format md --out .contextopt/context-pack.md
```

Then read `.contextopt/context-pack.md` and summarize project shape, likely entrypoints, high-signal files, parser coverage, and files to read next.
